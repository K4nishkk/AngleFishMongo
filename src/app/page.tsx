'use client'

import FolderView from "@/components/folderView";

const Home = () => {
  const createdAt = new Date();
  const updatedAt = new Date();

  return (
    <FolderView />
  );
};

export default Home;